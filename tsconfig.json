{
  "compilerOptions": {
    "target": "esnext",                   // 컴파일된 자바스크립트의 목표 버전
    "module": "commonjs",                 // 모듈 시스템 (일반적으로 commonjs나 ES 모듈을 선택)
    "lib": ["dom", "dom.iterable", "esnext"],  // 사용할 라이브러리
    "allowJs": true,                      // JavaScript 파일을 허용
    "jsx": "preserve",                    // JSX 구문을 유지 (Babel을 사용하여 처리)
    "skipLibCheck": true,                 // 라이브러리 검사를 건너뛰기
    "strict": true,                       // 엄격한 타입 체크 활성화
    "esModuleInterop": true,              // CommonJS 모듈과 ES 모듈 간의 상호 운용성 활성화
    "allowSyntheticDefaultImports": true, // default import 문법을 허용
    "forceConsistentCasingInFileNames": true,  // 파일 이름의 일관된 대소문자 강제
    "moduleResolution": "node",           // 모듈 해결 방법 설정
    "resolveJsonModule": true,            // JSON 모듈을 해결하기 위해 필요한 설정
    "isolatedModules": true,              // 모듈을 단독으로 컴파일
    "noEmit": true                        // 컴파일된 파일을 생성하지 않음
  },
  "include": [
    "next-env.d.ts",                      // Next.js 환경 설정 파일
    "**/*.ts",                            // TypeScript 파일
    "**/*.tsx"                            // TypeScript + JSX 파일
  ],
  "exclude": ["node_modules"]              // 컴파일에서 제외할 디렉토리
}